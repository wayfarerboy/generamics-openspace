# A sample Guardfile
# More info at https://github.com/guard/guard#readme

#guard :process, :name => 'Clear cache', :command => 'drush @openspace.local cc all' do
#  watch(%r{^.+\.php$})
#end

#guard :livereload, :host => '192.168.1.65', :port => '35729' do
  #watch(%r{^css\/.+\.css$})
  #watch(%r{^js\/.+\.(js|pde)$})
  #watch(%r{^.+\.php$})
#end

guard 'compass' do
  watch(%r{^sass\/.+\.s[ac]ss$})
end
